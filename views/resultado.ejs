<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Resultados de la búsqueda: <%= search %></h2>
			</div>
			<!-- Show the "visited" products -->
			<% for(let producto of productos ) { %>
	
				<div class="col-12 col-sm-6 col-lg-3">
					<section class="product-box">
						<a href="/products/detail/<%= producto.id %>">
							<figure class="product-box_image">
								<img src="/images/<%= producto.imagen %>" size="5px">
							</figure>
							<article class="product-box_image">
								<h2>$<%= producto.precio %></h2>
								<span><%= producto.descripcion %> % OFF</span>
								<p><%= producto.talle %></p>
								<i class="fas fa-truck"></i>
							</article>
						</a>
					</section>
				</div>
				
				<% } %>

				<% if(productos.length == 0) {%>
					<div class="col-12">
						<h2 class="products-title" style="color: tomato;">No hay productos que coincidan con esa búsqueda</h2>
					</div>
				<% } %>
		</div>
	</div>
    
</body>
</html>