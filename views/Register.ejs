<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/stylesregister.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <title>Registro</title>
</head>
<body>
   <header>
   </header>
    <div class="formulario">
    <h2>
       Registrate 
    </h2>
    </div>
      <% if (locals.errors) {
         errors.forEach(error => { %>
            <p><%= error.msg %></p>
         <% })
         } %>
         <form action="/users/register" method="POST" enctype="multipart/form-data">
            <div class="form-data">
              <label class="dato-numero" for="Nombre">NOMBRE COMPLETO: </label>
              <input
                  type= "text"
                  name="nombre"
                  id="nombre"
                  value="<%= locals.old && old.nombre ? old.nombre : '' %>"
                  class="form-control <%= locals.errors && errors.nombre ? 'is-invalid' : null %>"
              <% if (locals.errors && errors.nombre) { %>
               <div class="text-danger">
                     <%= errors.nombre.msg %>
               </div>
              <% } %>
            </div>         
     <br>
     <label class="dato-numero" for="Fecha de Nacimiento">FECHA DE NACIMIENTO</label>
     <input type="date" name="nacimiento" value="2000-01-01" min="1900-01-01" max="2100-01-01 value="<%= locals.old && old.nacimiento ? old.nacimiento : '' %>">
     <br>
     <label class="dato">SELECCIONA GENERO</label>
     <select class="dato-genero" name="genero">
        <option value="Masculino" <%= locals.old && old.category === 'Masculino' ? 'selected' : ''; %>Masculino</option>
        <option value="Femenino" <%= locals.old && old.category === 'Femenino' ? 'selected' : ''; %>Femenino</option>
        <option value="Indefinido"<%= locals.old && old.category === 'Indefinido' ? 'selected' : ''; %>Indefinido</option>
     </select>
     <br>
     <label class="dato-numero" for="celular">CELULAR</label>
     <input type="number" name="celular" id="celular" value="<%= locals.old && old.celular ? old.celular : '' %>">
     <br>
     <label class="dato" for="Email">EMAIL</label>
     <input type="email" name="email" id="Email" value="<%= locals.old && old.email ? old.email : '' %>">
     <br> 
     <label class="dato" for="Usuario">USUARIO </label>      
     <input type="text" name="usuario" id="Usuario" value="<%= locals.old && old.usuario ? old.usuario : '' %>">
     <br>
     <label class="dato" for="Contraseña">CONTRASEÑA </label>
     <input type="password" name="password" id="Contraseña" value="<%= locals.old && old.password ? old.password : '' %>">
     <br>   
     <label class="dato" for="Repetir Contraseña">REPETIR CONTRASEÑA </label>
     <input type="password" name="repetir" id="repetir Contraseña" value="<%= locals.old && old.repetir ? old.repetir : '' %>">
     <br>
     <label class="dato-numero" for="foto">FOTO PERFIL </label>
     <input type="file" name="imagen" id="foto">
     <br>
     <div class="botonera">
              <button class="botones" type="reset">Borrar</button> 
         <button class="botones" type="submit">Registrate</button>
         <p> ¿YA TENES CUENTA? </p> <a class="hiper" href="/login"> INGRESA  AQUI </a>
     </div>             
   </div>
   <div class="banner">
      <img src="images/ova-logo.jpg" alt="logotipo">
   </div>
</form>
   <%- include("partials/footer") %>
 </body>
</html>